import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mainContainer: {
    flex: 1,
    backgroundColor: "#F1F3F6",
  },
  safeAreaView: {
    paddingTop: 14,
    marginHorizontal: 16,
  },
});
