import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  mainContainer: {
    borderWidth: 1,
    gap: 16,
  },
  formHeading: {
    lineHeight: 17.6,
    fontSize: 16,
    fontWeight: "500",
  },
});
